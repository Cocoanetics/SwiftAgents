.DS_Store
.build/
.swiftpm/
build/
xcuserdata/
*.xcuserstate
Package.resolved
.claude

# XcodeGen-generated artifacts (Examples/RealtimeVoiceAgent uses project.yml
# as the source of truth — run `xcodegen generate` to materialise the project).
*.xcodeproj
DerivedData/

# Secrets — never commit
.env
.env.*
!.env.example
Local.xcconfig
